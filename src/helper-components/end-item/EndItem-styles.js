
import Color from '../../services/random-color/randomColor.js';

const EndItemStyles = {
    endItem: {
        backgroundColor: Color.greyColor,
        // height: 339,
        flex: 1
    }
}

export default EndItemStyles;
